services:
  - type: web
    name: logo-preview-editor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --host 0.0.0.0 --port $PORT logo_preview_editor:app
